export default function PageNotFound() {
  return (
    <h1
      style={{
        flexDirection: "column",
        alignItems: "center",
        display: "flex",
      }}
    >
      PageNotFound
    </h1>
  );
}
